# Секреты и ключи
.env
.env.local

# ДАННЫЕ МАГАЗИНА — НИКОГДА не в гите.
# Именно это гарантирует, что деплой физически не может затереть каталог.
data/
catalog.json
public/catalog.json

# Служебное
node_modules/
*.log
.DS_Store
tmp/
